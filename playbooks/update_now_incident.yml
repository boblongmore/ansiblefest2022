---
- name: update servicenow incident
  hosts: localhost
  gather_facts: false

  tasks:
    - name: make router interface document
      ansible.builtin.template:
        src: templates/rtr-int-report.j2
        dest: documentation/rtr-int-report.md
      run_once: True

    - name: Change state of the incident
      servicenow.itsm.incident:
        number: "{{ incident_id }}"
        attachments:
         - path: documentation/rtr-int-report.md
           #path: router-ospf.md
           type: text/markdown
        state: in_progress
        other:
          comments: attached router int report

